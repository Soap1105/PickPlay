# PickPlay 2학기 0주차 개발노트

> 작성일: 2026-08-26 ~ 2026-09-01  
> 목표: 방학 중 UI/UX 가시적 개선 및 2학기 본 개발 과제 실태 전수 점검

---

## ✅ 0주차 완료 항목

### 1. 로딩 화면
- raw HTML 순간 노출 방지용 전체 오버레이 삽입
- 소켓 `role update` 수신 시 최소 1.5초 유지 후 페이드아웃, 끊김 시 5초 재연결 카운트다운
- 디자인: PickPlay 로고 + 3점 펄스 애니메이션 (A안 미니멀)
- 다크/라이트 테마 변수 100% 호환 (배경·로고·점 색상 자동 전환)
- `game.html` / `common.css` / `room.js` 수정
- **상태**: ✅ 완료 (2026-08-26 / 테마 연동 2026-09-01)

---

### 2. 화면 너비 밸런스 최적화 (골디락스 존)
- `#main-wrapper` → `max-width: 1780px` (FHD 풀스크린 안정감 + QHD/고해상도 프레임 밸런스 확보)
- 우측 사이드바 `#right-sidebar` → `max-width: 340px` / `min: 280px` 캡핑 적용
- `common.css` 수정
- **상태**: ✅ 완료 (2026-08-31)

---

### 3. 다크 / 라이트 테마 전환 시스템 (쿨 슬레이트)
- 채팅 헤더에 ☀️/🌙 토글 버튼 탑재, `localStorage` 자동 동기화
- **`:root` = 라이트(쿨 슬레이트) 기본값**, `body.dark-mode` = 다크 테마 오버라이드 구조 확립
- 전체 레이아웃(헤더·채팅·모달·로비·인게임 배경·토스트·로딩창) CSS 변수 매핑 완료
- 테마 전환 타이밍: **즉시 snap** (partial transition 제거로 딜레이 텀 완전 해결)
- `game.html` / `common.css` / `room.js` 수정
- **상태**: ✅ 완료 (2026-09-01)

---

### 4. 게임 목록 카드 UI/UX 개선
- **인원 수 가시성 강화**: `game-tag-badge`에 게임별 고유 색상 + `font-weight: 900` + 컬러 보더 적용하여 필수 조건 최우선 강조
- **게임별 컬러 틴트 배경**: 게임 카드 안쪽에 고유 틴트(빙고=블루, 라이어=레드, 폭탄=오렌지) 기본 적용
- 호버 시 틴트 강화 + 2px 리프팅, 선택(active) 시 링 글로우
- 게임 제목(`game-title-text`) 인라인 제거 ➡️ CSS 클래스로 라이트/다크 명도 최적화
- **상태**: ✅ 완료 (2026-09-01)

---

### 5. 로비 렌더링 성능 최적화
- 로비 패널 3개(게임 목록·파티원·게스트 대기)의 불필요한 `backdrop-filter: blur()` 제거 ➡️ 체감 렉 완전 해소
- **상태**: ✅ 완료 (2026-09-01)

---

## 🔍 코드베이스 전수 검사 결과 (기존 완료 항목)
- [x] **빙고 체크 스탬프 바운스 & 라인 완성 연출**: `bingo.css` (122~167줄) `cellCheckStamp`, `cellBingoStamp` 이미 구현 완료 확인
- [x] **빙고 단어 폰트 반응형 최적화**: `bingo.css` `clamp()` 이미 적용 완료 확인

---

## 📋 2학기 본 개발 잔여 과제 (1주차 착수 목록)

| # | 항목 | 구분 | 위치 | 설명 | 우선순위 |
| :---: | :--- | :---: | :--- | :--- | :---: |
| **1** | **폭탄 지목 번호 배지 추가** | 폭탄 | `public/js/bomb.js`, `bomb.css` | 아레나 플레이어에 `${playerNum}` 번호 뱃지 부여하여 전략 모드 지목 편의성 확보 | ✅ 완료 (2026-09-02) |
| **2** | **라이어 자기 투표 오류 문구 수정** | 라이어 | `sockets/liarSocket.js` (339줄) | `"자신의 힌트에는 투표할 수 없습니다."` ➡️ `"본인에게는 투표할 수 없습니다."`로 문구 정정 | ✅ 완료 (2026-09-02) |
| **3** | **라이어 게스트 대기 안내 문구 보완** | 라이어 | `public/js/liar.js` (812줄) | 불필요한 장식 배제하고 `"방장이 다음 라운드를 준비하고 있습니다."`로 명확화 | ✅ 완료 (2026-09-02) |
| **4** | **폭탄 하트 렌더링 함수화** | 폭탄 | `public/js/bomb.js` | 중복 하트 이모지 루프를 `getHeartHtml()` 헬퍼 함수로 통합 | 🟡 중우선 |
| **5** | **한글 Room ID 코드 도입** | 공통 | `sockets/commonSocket.js`, `server.js` | 난수 영문 `room-x4fyneb` ➡️ 기억하기 쉬운 `행복한-호랑이-12` 한글 방 코드 도입 | 🟡 중우선 |
| **6** | **게임 카드 아이콘 에셋 교체** | 공통 | `public/games/game.html` | 수작업 캐릭터 에셋 또는 정식 SVG 엠블럼 완성 시 교체 | ⚪ 보류 |
